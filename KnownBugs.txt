Bugs:
- Icon attribution is required. Author is Streamline and website is https://www.streamlinehq.com/. Website attribution is
  mandatory.
- Pan tool shouldn't process clicks when a zoom is in progress.
- Zoom tool should not process middle mouse zooms while the left mouse button is pressed.
- Box counting preview has bottom side cropped.
- When the main fractal is panned while in Julia mode, the C constant is not updated.

Improvements to get to a release version:
- Main frame must have tools to navigate and to get information about a point.
- Status bar precision should be adjusted to the current zoom level.
- Julia mode must have an "Iterations" display, must have a menu bar to switch the main fractal to a julia set on the
specific current point.
- Documentation must be updated.
- Localization to Spanish, Japanese and possibly other languages.
- "Set color" on RendererOptions must be a color picker.
- Replace the bmp banners at fractal options and RendererOptions.
- Restore Henon maps and other fractals of previous versions.
- Implement a proper tutorial mode.
- Icon must be updated.