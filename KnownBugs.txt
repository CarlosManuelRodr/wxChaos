- Incomplete renders get saved on the zoom cache and set while zooming back.
- Zoom recorder is not performing the zoom at the depth level that the user has zoomed in to.
- Changing the gradient color palette with the relative color option active should invalidate the cache and redraw everything.

Improvements:
- Implement render passes. This will make it possible to zoom faster.
- wxWidgets and Angelscript should be imported from Vcpkg.
- muParserX should be imported as a submodule to benefit from the latest bug fixes.
- There is an update of https://wxmathplot.sourceforge.io/ that should be used instead of the old one. Since the author
  doesn't use git I will have to keep downloading it manually and add it to third_party.